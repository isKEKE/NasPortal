fastapi
uvicorn[standard]
jinja2
sqlalchemy
bcrypt==4.0.1
passlib==1.7.4
python-multipart  # For form data
python-jose[cryptography] # For JWT or secure sessions (example uses placeholder)
databases[sqlite] # Or databases[postgresql], databases[mysql] etc.
alembic # For database migrations (recommended but not shown in code)
pydantic[email] # For email validation if needed in schemas
typer[all]
psycopg2
gunicorn